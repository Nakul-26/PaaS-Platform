// Package runtime defines the ContainerRuntime interface (ADR-0006) used by
// the worker agent to manage containers, independent of the underlying
// container engine, and the Docker Engine SDK adapter that implements it.
package runtime
