// Package eventbus defines the EventBus interface (ADR-0005, ADR-0011) for
// internal pub/sub and work distribution. The NATS adapter is introduced in
// Phase 2 — Phase 1 has a single worker called directly over HTTP.
package eventbus
