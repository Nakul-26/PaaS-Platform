# Roadmap & Phase Status

Living tracker — update this file as phases progress. Design decisions live in `ARCHITECTURE.md` and `docs/adr/`; this file only tracks status. Detailed exit criteria for each phase are in `ARCHITECTURE.md` §10; Phase 1 additionally has a full task breakdown in [`docs/phases/phase-1-mvp.md`](docs/phases/phase-1-mvp.md), Phase 2 in [`docs/phases/phase-2-multi-node.md`](docs/phases/phase-2-multi-node.md), Phase 3 in [`docs/phases/phase-3-controllers.md`](docs/phases/phase-3-controllers.md), Phase 4 in [`docs/phases/phase-4-service-discovery-lb.md`](docs/phases/phase-4-service-discovery-lb.md), and Phase 5 in [`docs/phases/phase-5-networking-ingress.md`](docs/phases/phase-5-networking-ingress.md).

Status legend: ⬜ Not started · 🟨 In progress · ✅ Done

| Phase | Name | Status | Notes |
|---|---|:---:|---|
| 0 | Architecture | ✅ | `ARCHITECTURE.md` + full `docs/` set written 2026-08-16. |
| 1 | Single-Node Container Platform (MVP) | ✅ | Started 2026-08-16, exit criteria automated and passing 2026-08-17. Tasks 1-8 done: repo/module scaffolding; Postgres migrations 0001-0006 with RLS (ADR-0010) + cross-tenant integration test; `ContainerRuntime` interface + Docker SDK adapter with lifecycle integration test; worker agent (separate process, HTTP contract in `docs/worker-agent-contract.md`) with lifecycle integration test driven purely over HTTP; API server core CRUD (JWT auth + rotating refresh tokens, default-org bootstrap, per-aggregate repositories, RBAC middleware, project/application/deployment routes driving the worker agent) with a full-flow integration test against real Postgres + a real worker process; `cobra`-based CLI (`apps/cli`, ADR-0007) — `signup`/`login`, `create project`, `deploy` (create-or-update + `--port` binding), `get deployments`, `logs`, `delete` — manually verified end-to-end against a real Postgres + worker + apiserver stack, including a real `nginx` container reachable via its mapped port; log retrieval (`GET /v1/applications/:appId/logs`, proxied through the API server to the worker's `StreamLogs`) making `platform logs`/`--follow` fully functional, covered by the Task 5 integration test's extension into a real deploy-then-read-logs step plus a cross-tenant denial check; the phase's exit-criteria script itself automated as `e2e/phase1_test.go` (`go test -tags=e2e ./e2e/...`) — builds the real `platform`/`apiserver`/`worker` binaries, runs them as separate OS processes against a real testcontainer Postgres, and drives the CLI binary through signup→create project→deploy nginx→poll for running→curl the mapped port→read logs→delete→confirm the container is actually unreachable afterward, passing in ~40s with no leaked containers. Along the way, fixed a bug the new `--follow` path would have hit: both `workerclient` and the CLI's `apiclient` had a blanket 30s `http.Client` Timeout that covers the *entire* response body read, which would have silently killed any log stream (follow or not) after 30 seconds — replaced with a per-call `context.WithTimeout` on non-streaming calls only, leaving streaming calls bounded solely by the caller's own context. |
| 2 | Multi-Node Infrastructure | ✅ | Started 2026-08-18, exit criteria automated and passing 2026-08-21: [`docs/phases/phase-2-multi-node.md`](docs/phases/phase-2-multi-node.md). Task 1 done: NATS added to `infrastructure/docker-compose.yml`; `EventBus` interface + NATS adapter (core + JetStream) in `internal/eventbus`; subjects/schemas documented in `docs/nats-contract.md`; round-trip integration test against a real NATS/JetStream container passing. Task 2 done: `0007_nodes_containers.sql` migration (`nodes`/`containers`, no RLS — infrastructure, not tenant-scoped); `NodeRepository`/`ContainerRepository` in `internal/db`; integration test against real Postgres covering register/re-register/heartbeat/status-transition flows passing. Task 3 done: `services/worker/internal/nodeagent` publishes `node.<id>.register` once at startup (node ID generated and persisted locally so restarts re-register the same node) then `node.<id>.heartbeat` on a configurable interval (default 5s); purely additive alongside the untouched HTTP contract, connects to NATS with a 30s retry-then-degrade-to-HTTP-only loop since the nats image has no exec healthcheck to depend on; integration test builds and runs the real worker binary against a real NATS container, confirming registration + scheduled heartbeats arrive. Task 4 done: the node agent now also subscribes durably (JetStream, `NODE_ASSIGNMENTS` stream) to its own `node.<id>.assign` subject; on message it drives the same `ContainerRuntime` pull/create/start path the HTTP `POST /v1/containers` handler uses, then publishes the outcome durably (`NODE_STATUS` stream) on `node.<id>.status`, mapping `internal/runtime.Status` onto `containers.status`'s vocabulary (`exited`+exit-code splits into `stopped` vs `crashed`); integration test publishes an assignment directly (no scheduler yet), confirms a real container starts on the real Docker daemon and a matching status message comes back. Task 5 done: new `services/scheduler` binary (`internal/scheduler` package) — a node registry consuming `node.*.register`/`node.*.heartbeat` (core NATS wildcard subscribe) to upsert `nodes` rows, plus a periodic liveness sweep marking a node `unreachable` once its `last_heartbeat_at` exceeds a configurable timeout (default 15s, swept every 5s); placement consuming `placement.requested` (JetStream `PLACEMENT` stream), applying filter-then-score (§2.2: filter unhealthy/zero-capacity nodes, score the rest by lowest current non-terminal-container count), writing a `containers` row (its own id doubling as `assignment_id`), and publishing `node.<id>.assign` onto the winning node — then consuming `node.*.status` back to update that row via `assignment_id`. NATS is treated as a hard dependency for this service (unlike the worker's degrade-to-HTTP-only), since the scheduler has no other job. Integration test builds and runs the real scheduler binary plus two real worker binaries against real Postgres + NATS + Docker, publishes a `placement.requested` event, and confirms exactly one `containers` row appears pointing at one of the two registered nodes, that its status reaches `running`, and that the resulting container is actually running on the real Docker daemon. Task 6 done: `handleDeploy` no longer calls the worker over HTTP — it writes the `deployments` row then publishes `placement.requested`, matching `ARCHITECTURE.md` §2.1; a publish failure marks the deployment `failed`, a successful publish leaves it `pending` and relies entirely on the scheduler/worker's own `containers` writes from here on. Added `GET /v1/nodes`, gated by a new `requirePlatformAdmin` check (an explicitly scoped approximation — owner/admin in any org — since no real platform-wide role exists yet). Extended the deployment read path with an `attachPlacement` join through `containers` to surface `node_id`/`container_status`. `deployment.WorkerContainerID` is now a permanent Phase 1 relic (`handleDeploy` never populates it again), which broke and required fixing three downstream consumers: `handleGetLogs`, `handleDeleteApplication`, and the CLI's `get deployments` table (now shows `NODE`/`CONTAINER_STATUS`). The API server's own NATS dependency degrades gracefully (nil bus) rather than failing hard like the scheduler's, since only the deploy route needs it. Both the API server's integration test and `e2e/phase1_test.go` were extended to run a real scheduler + NATS alongside Postgres/worker, fixing along the way a startup-ordering bug (the scheduler's `node.*.register` subscription must be live before the worker's one-shot core-NATS registration publish, or it's lost forever — core NATS has no replay). Known, deliberately deferred gap found in already-shipped Task 5 code: `registry.go`'s heartbeat-for-unregistered-node handler logs "will self-heal on next register/heartbeat" but doesn't actually re-register anything — it just drops the message; a real fix needs new design work since the heartbeat payload lacks the data required to re-register. Task 7 done: added `platform get nodes`, listing every registered node's `ID`/`HOSTNAME`/`STATUS`/`LAST_HEARTBEAT_AT` via the CLI's new `apiclient.ListNodes` calling `GET /v1/nodes`. Unlike `get deployments`, this isn't project-scoped (visibility is gated server-side by `requirePlatformAdmin`, not org/project membership), so it only requires a logged-in session (`requireLogin`), not `requireProject`. Task 8 done: `infrastructure/docker-compose.yml` now brings up `worker-1`/`worker-2`/`worker-3` (new `services/worker/Dockerfile`, multi-stage build), each with a distinct artificial CPU/memory capacity ceiling (2000m/2048MB, 1000m/1024MB, 3000m/3072MB) per ADR-0009, all pointed at `nats` and sharing the host's real Docker daemon via a mounted `/var/run/docker.sock` — not isolated hosts, exactly as the ADR documents. Each worker gets its own named volume for `WORKER_NODE_ID_FILE` so a restart re-registers the same node identity rather than minting a new one. Verified live: `docker compose up nats worker-1 worker-2 worker-3` brings up all three, each registers over NATS with a distinct node ID/hostname within the same tick, and a real `POST /v1/containers` call against `worker-1` actually pulled and started `nginx:latest` on the host daemon through the mounted socket — confirming the socket mount, not just the process wiring, actually works. apiserver/scheduler remain host processes for now (only `postgres`/`nats`/the 3 workers are containerized) — matching Task 8's literal scope, not `docs/local-development.md`'s full aspirational service list. Task 9 done: automated this phase's own exit-criteria script as `e2e/phase2_test.go` (`TestE2E_Phase2ExitCriteria`, `go test -tags=e2e ./e2e/...`), mirroring Phase 1's exit-criteria test's real-binaries-as-real-OS-processes discipline — builds and runs a real `apiserver`, `scheduler`, and 3 real `worker` processes against real testcontainer Postgres + NATS, all driven through the compiled `platform` CLI binary: deploy nginx → poll `get deployments`/`get nodes` until the scheduler places it on one of the 3 (confirming all 3 report `healthy` first) → hard-kill that specific worker's OS process → poll `get nodes` until just that node reports `unreachable` while the other two stay `healthy`. Passes reliably in ~50s (verified with repeated fresh runs, `-count=1`). Sped up the scheduler's liveness sweep (`SCHEDULER_HEARTBEAT_TIMEOUT=3s`/`SCHEDULER_LIVENESS_SWEEP_INTERVAL=1s`) and the worker's heartbeat (`WORKER_HEARTBEAT_INTERVAL=1s`) via their existing env-var tunables so the test doesn't have to eat the 15s/5s production defaults. Along the way, found and fixed a real container leak: unlike Phase 1's script, this phase's exit-criteria script has no delete step, so the deployed nginx container would otherwise leak on every test run (confirmed happening before the fix); added a before/after `docker ps` diff in the test itself (shelling out to the `docker` CLI, not the SDK — ADR-0011 reserves direct Docker client imports for `internal/runtime` alone) that force-removes exactly the one container this test created. Also confirmed, while diagnosing that leak, a real latent bug worth flagging separately: `handleDeleteApplication`'s post-Task-6 fix routes container stop/remove through the single fixed `WORKER_ADDR` HTTP client regardless of which node a deployment actually landed on — in a true multi-node deployment (unlike this test, where nodes conveniently share one Docker daemon), deleting an application placed on a node other than the one `WORKER_ADDR` points at would silently no-op (the wrong worker's "container not found" is treated as an acceptable outcome) rather than actually stopping the real container. Not fixed here — out of Task 9's scope, needs `handleDeleteApplication` to route through the deployment's actual placed node instead of a single hardcoded worker address, which likely wants its own tracked task. Phase 2's exit criteria (`ARCHITECTURE.md` §10) now pass end-to-end via this automated test — phase marked done. |
| 3 | Desired State + Controllers | ✅ | Started 2026-08-21, exit criteria automated and passing 2026-08-22: [`docs/phases/phase-3-controllers.md`](docs/phases/phase-3-controllers.md). Task 1 done: `nodeagent` now tracks an in-memory `assignment_id → container_id` map on successful assignment and polls `ContainerRuntime.ContainerStatus` on a configurable interval (`WORKER_HEALTH_CHECK_INTERVAL`, placeholder default 3s), republishing `node.<id>.status` whenever a tracked container's observed status changes (`running` → `crashed`/`stopped`) — closing the gap where nothing watched a container after its first status message. Task 2 done: new `node.<id>.unassign` JetStream subject (`docs/nats-contract.md`) — worker subscribes durably, stops/removes the named container via the existing `ContainerRuntime` path, publishes a final `status: "stopped"`, and drops it from Task 1's tracking map. Task 3 done: `ContainerRepository.CountActiveByDeployment` (pending+running count, the controller's "actual" side) and `ApplicationRepository.UpdateReplicas`; no migration needed since `containers.deployment_id` already permits multiple rows. Task 4 done: `services/controller-manager` replaced its stub with a real Deployment controller (`internal/controller`) — on each tick (`CONTROLLER_RECONCILE_INTERVAL`, placeholder default 5s) it compares `replicas_desired` against Task 3's active count per running deployment, publishing `placement.requested` for a shortfall (reusing the scheduler's existing placement path unchanged) or `node.<id>.unassign` oldest-created-first for an excess; connects to Postgres as the superuser (`DATABASE_URL`), a documented RLS-bypass exception (`internal/db.ReconcileRepository`'s doc comment) since it reconciles across every tenant with no per-request session to scope RLS off of; single-instance only, no leader election yet (mirrors R3's posture for the Phase 2 scheduler). Task 5 done: `PATCH /v1/applications/:id/scale` + `platform scale <app> --replicas N` — updates `replicas_desired` only, no synchronous placement; the controller's next tick treats it identically to crash recovery, no scale-specific code path. Task 6 done: `handleDeleteApplication` re-routed from a single hardcoded `WORKER_ADDR` HTTP call to a `node.<id>.unassign` publish per container addressed to that container's actual `node_id` (`ContainerRepository.ListByDeployment` already had the right data — only the dispatch target was wrong), fixing the Phase 2 Task 9 flagged gap for real multi-node deletes. Task 7 done: `get deployments` now shows a `REPLICAS` column (`N/M` running/desired) instead of assuming one container per deployment. Task 8 done: the phase's own exit-criteria script automated as `e2e/phase3_test.go` (`TestE2E_Phase3ExitCriteria`, `go test -tags=e2e ./e2e/...`) — builds and runs real `apiserver`, `scheduler`, `worker`, and (new) `controller-manager` processes against real testcontainer Postgres + NATS + the real Docker daemon, all driven through the compiled `platform` CLI: deploy nginx → poll `get deployments` to `1/1` → identify the actual container via the same before/after ancestor-image `docker ps` diff Phase 2's test uses (no CLI surface exposes a raw container ID) → `docker kill` it directly (not the worker process) → poll down to `0/1` (crash noticed) → poll back up to `1/1` (replacement placed and running), confirmed by grep that containers carry no Docker restart policy so the recovery signal is genuinely the controller's, not Docker's. Passed in ~71s on first run; sped up `WORKER_HEALTH_CHECK_INTERVAL`/`CONTROLLER_RECONCILE_INTERVAL` to 1s each via existing tunables, same reasoning as Phase 2's interval speed-ups. `startPostgres` (shared by all three e2e tests) now returns both the admin/superuser DSN and the `platform_app`-role DSN to support controller-manager's role split. Phase 3's exit criteria (`ARCHITECTURE.md` §10) now pass end-to-end via this automated test — phase marked done. |
| 4 | Service Discovery + Load Balancer | ✅ | Started 2026-09-02, exit criteria automated and passing 2026-09-02: [`docs/phases/phase-4-service-discovery-lb.md`](docs/phases/phase-4-service-discovery-lb.md). Task 1 done: `0008_services_service_instances.sql` migration (`services`/`service_instances`, no RLS — cluster runtime state, same precedent as `nodes`/`containers` — plus the `service_instances(service_id, healthy)` index the load balancer's resync query needs); `ServiceRepository`/`ServiceInstanceRepository` in `internal/db` (create-or-get by `application_id`, upsert/list-healthy/mark-unhealthy-or-delete by `container_id`); repository-level integration tests against real Postgres. Task 2 done: `ContainerStatusInfo` (`internal/runtime`) now carries the container's actual bound host port, read from the same `ContainerInspect` call's `NetworkSettings` `DockerRuntime.ContainerStatus` already makes (no new Docker API call); `node.<id>.status`'s payload (`docs/nats-contract.md`) extended with the resolved port so a downstream consumer never has to query Docker itself — `nodes.ip` (Phase 2) plus this port is the full reachable address; integration test starts a real container with an ephemeral host port and confirms the actual assigned port comes back, not 0. Task 3 done: a second controller-manager reconcile loop (Service Instance controller, `internal/controller`) alongside Phase 3's Deployment controller, same single-instance posture (R3) — each tick joins `containers`+`nodes` for every running deployment, lazily creates a `services` row on an application's first healthy instance (`dns_name` format `<project-slug>-<app-slug>.internal`, open decision 1), and upserts/removes `service_instances` rows to match; publishes `service.updated` on core NATS (not JetStream, per §2.6's own reasoning — Task 4's periodic resync is the required reconciliation pairing); integration test against real Postgres+NATS+a real worker confirms 3 replicas produce 3 healthy `service_instances` rows and stopping one flips it unhealthy/removed with an event firing. Task 4 done: `services/loadbalancer` replaced its Phase 0 stub with a real reverse proxy — in-memory registry populated via `service.updated` subscribe plus a periodic full Postgres resync fallback (never queried on the request hot path itself, §2.5/§2.6); round-robin strategy behind a `BalancingStrategy` interface (`services/loadbalancer/internal/strategy`, per `docs/modularity-and-extensibility.md`'s named seam) leaving room for weighted/least-connections later; requests matched on the `X-Platform-Service: <dns_name>` header (open decision 2 — the phase's own suggested simplest option, since real hostname routing is Phase 5's job) and reverse-proxied via `httputil.NewSingleHostReverseProxy` (transparently handles WebSocket upgrade too, no extra code needed); integration test against real Postgres+NATS+3 real `traefik/whoami` backend containers (chosen because its response body names the exact container that served a request, the only way to observe round-robin distribution from outside) confirms repeated requests hit all 3. Task 5 done: unified passive/active/TTL health tracking into one `unhealthyUntil` mechanism on the registry (`MarkUnhealthy`/`MarkHealthy`) rather than parallel structures — TTL sweep drops an instance not refreshed within a configurable window (`LOADBALANCER_INSTANCE_TTL`), a proxied request that errors ejects its backend for a configurable window (`LOADBALANCER_EJECTION_WINDOW`) independent of the registry, and a new `internal/healthcheck` package actively probes every registered instance on a configurable interval/path (`LOADBALANCER_HEALTH_CHECK_INTERVAL`/`_PATH`); integration test stops a replica's container process directly (not via scale-down, with the Deployment/Service-instance controller reconcile intervals deliberately decoupled so the assertion proves the load balancer's own active check — not a controller tick — is what removes it) and confirms it drops from rotation within one health-check interval while survivors keep serving; a container-leak found mid-development (a controller-scheduled replacement outliving the test) was fixed by re-scanning and registering cleanup after the assertion window, not just at the initial snapshot. Task 6 done: extended the existing `get deployments` read path (not a new `platform get services` command, per the phase doc's own instruction) — the API's `deploymentResponse` gained an application-level `service.dns_name` field (nil until Task 3 lazily creates one), attached per-row the same way `attachReplicaState` already attaches `ReplicasDesired`; the CLI's `apiclient.Deployment` and `get deployments` table gained a matching `SERVICE` column; `TestAPIServer_ScaleApplication` extended to assert the surfaced `dns_name` ends in `.internal`. Task 7 done: the phase's own exit-criteria script automated as `e2e/phase4_test.go` (`TestE2E_Phase4ExitCriteria`, `go test -tags=e2e ./e2e/...`) — builds and runs real `apiserver`, `scheduler`, `worker`, `controller-manager`, and (new) `loadbalancer` processes against real testcontainer Postgres+NATS+the real Docker daemon, all driven through the compiled `platform` CLI: deploy `traefik/whoami` → `scale --replicas 3` → poll `3/3` → read the SERVICE column's `dns_name` → send repeated requests through the load balancer confirming all 3 backends are hit → `docker kill` one replica's container directly → poll through the load balancer over an 8×health-check-interval window confirming the killed backend stops being routed to while a survivor keeps serving. Passed on first run (~197s), no leaked containers, full module `gofmt`/`go build`/`go vet` (default, `-tags=integration`, and `-tags=e2e`) all clean. Phase 4's exit criteria (`ARCHITECTURE.md` §10) now pass end-to-end via this automated test — phase marked done. |
| 5 | Networking + Ingress | ✅ | Started 2026-09-02, exit criteria automated and passing 2026-09-03: [`docs/phases/phase-5-networking-ingress.md`](docs/phases/phase-5-networking-ingress.md) task plan written and approved. Task 1 done: `0009_domains.sql` migration (`domains`, RLS-enabled — tenant-authored config, unlike Phase 4's `services`/`service_instances` — same two-branch policy shape as `applications`); `DomainRepository` in `internal/db` (Create/ListByProject/OrgID/Delete, RLS-bound); `DomainRoutingRepository` (`ListRoutes`, the `domains`+`services` cross-tenant join for the load balancer's Task 4 resync) — resolved to run over a second, narrowly-scoped admin connection mirroring `ReconcileRepository`'s existing pattern, rather than widening the load balancer's platform_app connection or dropping RLS from `domains`. Integration test against real Postgres covers CRUD, duplicate-hostname conflict, RLS cross-tenant isolation, and the admin-connection join. Along the way, narrowed Phase 5's own scope: path-based routing (also named in `ARCHITECTURE.md` §10) moved to the phase doc's deferred list — the exit criteria only tests hostnames, and a real implementation needs schema work (composite uniqueness, longest-prefix matching) not worth building speculatively. Task 2 done: `PermDomainsManage` (`domains.manage`) added to `internal/auth/rbac.go`'s matrix (owner/admin/developer allowed, viewer denied) and its unit test; three routes added to `services/apiserver` — `POST /v1/projects/:projectId/domains` (deep-by-ID, resolves org from the project, gated behind `PermDomainsManage`, and additionally confirms the caller-supplied `application_id` actually belongs to this project, not merely this org, before creating), `GET /v1/projects/:projectId/domains` (membership-only, no domains.manage needed — same precedent as deployments/logs), `DELETE /v1/domains/:domainId` (deep-by-ID via `DomainRepository.OrgID`, gated behind `PermDomainsManage`). New `TestAPIServer_Domains` integration test (Postgres only — domains never touch NATS/workers) covers the full CRUD path, duplicate-hostname 409, application-not-in-this-project 404, cross-tenant denial (404, not 403, on create/list/delete) per `rbac-multitenancy.md` §5, and the permission-matrix check (developer allowed, viewer denied on both create and delete) — the latter seeded by inserting membership rows directly (no invite route exists yet) via the owner's own admin session, per `is_org_admin`'s existing allowance in the memberships RLS policy. `POST` also sets `tls_status` to `active` synchronously right after `Create` (open decision 3: `DomainRepository.Create` itself still defaults to `pending`, the schema's general-purpose default; the new `UpdateTLSStatus` method and the active-flip belong to the handler, since Task 5's self-signed adapter has no real provisioning latency to model) — a gap caught and fixed against the phase doc's own text while starting Task 3, before the user had reviewed it; both the repository-level and API-level integration tests updated to match (repository-level Create still asserts `pending`; the API-level assertion now checks `active`). Passed on first run (15.16s, re-verified 9.29s after the fix); full module `gofmt`/`go build`/`go vet` (default, `-tags=integration`, `-tags=e2e`) and `go test ./...` all clean. Task 3 done: `apiclient.Client` gained `CreateDomain`/`ListDomains`/`DeleteDomain`; `platform create domain <hostname> --app <app-name>` (resolves `--app` via `cfg.Applications`, same as `deploy`/`scale`) added under the existing `create` parent; `platform get domains` (project-wide, no app-name argument — unlike `get deployments`) added under `get`, resolving `application_id` back to a name via `cfg.Applications` for display when known; `platform delete domain <hostname>` added as a child of the existing flat `platform delete <app-name>` (`delete.go`) rather than restructuring it — cobra only dispatches to a subcommand when the first positional arg matches its name, so `platform delete demo` (Phase 1's literal, still-load-bearing exit-criteria invocation) falls through unchanged to `delete.go`'s own `RunE`; domain deletion has no local ID cache (unlike `cfg.Applications`), so it lists the current project's domains and matches by hostname client-side. Acceptance was the phase doc's own lighter bar for this task ("manual/integration check against a real apiserver," not a required permanent automated suite — Task 6's `phase5_test.go` is where the permanent e2e coverage for domain creation lives): verified with a throwaway e2e-style test (real Postgres+NATS+apiserver, the real compiled `platform` binary) driving the full flow — signup → create project → deploy → `create domain --app` → `get domains` (shows hostname/app name/`active` status) → `delete domain` → `get domains` (empty) → confirmed the legacy flat `delete demo` still works — passed (30.22s), then deleted per its own stated throwaway intent. Full module `gofmt`/`go build`/`go vet` (default, `-tags=integration`, `-tags=e2e`) and `go test ./...` all clean. Task 4 done: `services/loadbalancer` now opens a second, superuser-role connection (`LOADBALANCER_ADMIN_DATABASE_URL`, defaulting the same way controller-manager's plain `DATABASE_URL` does) purely for `db.DomainRoutingRepository`, alongside the untouched `platform_app`/`APP_DATABASE_URL` connection Phase 4 already uses for everything else; the periodic resync (Task 4 of Phase 4, unchanged ticker/interval, no new NATS subject per open decision 2) now also rebuilds a `hostname -> dns_name` table on the registry (`ReplaceHostRoutes`/`ResolveHost`, mirroring `ReplaceAll`'s ticker-only-refresh posture) independently of the services/service_instances half — a failure in either query leaves that half's previous contents in place without blocking the other. `proxy.ServeHTTP` now matches primarily on `r.Host` (port suffix stripped), falling back to the existing `X-Platform-Service` header when the Host isn't a registered domain, 404 when neither matches (previously 400 when the header alone was missing — no test relied on that exact code, and the phase doc's own acceptance criterion is explicitly 404). New `TestLoadBalancer_RoutesByHostHeader` integration test (two applications, one domain each, real containers) confirms each `Host` header reaches its own distinct backend and an unregistered `Host` gets 404; Phase 4's own two loadbalancer integration tests re-verified passing against the new admin-connection requirement (`startTestLoadBalancer` extended to accept and pass it). All three loadbalancer integration tests passed (one hit a transient NATS-container startup flake on its first attempt — retried clean, same known Docker Desktop flakiness precedent as earlier phases); no container leaks. Full module `gofmt`/`go build`/`go vet` (default, `-tags=integration`, `-tags=e2e`) and `go test ./...` all clean. Task 5 done: `services/loadbalancer/internal/tls` (package path and `CertProvider` interface name fixed by `docs/modularity-and-extensibility.md`'s registry row) — `CertProvider.GetCertificate(hostname string) (*tls.Certificate, error)`, and `SelfSigned`, Phase 5's only adapter (a real ACME/Let's Encrypt/Cloudflare adapter stays a documented future alternative): generates an ECDSA P-256 self-signed leaf cert for a hostname the first time it's asked for one, caches it in memory (never touches Postgres, mirroring how the registry itself works), 10-year validity since there's no rotation/renewal path yet and a self-signed cert needs no CA trust chain to keep working against an `InsecureSkipVerify` client — the only kind Phase 5 is built to serve. `main.go` gained a second `http.Server` on `LOADBALANCER_TLS_LISTEN_ADDR` (default `:8443`) sharing the exact same `proxy.Handler` instance as the plain-HTTP listener (already safe for concurrent use), whose `tls.Config.GetCertificate` resolves the handshake's SNI (`ClientHelloInfo.ServerName`) through the `SelfSigned` provider — rejecting a handshake with no SNI outright rather than guessing a default cert. Plain HTTP keeps listening unchanged alongside it, no forced redirect (open decision 4) — both were verified reaching the identical backend for the same hostname. New `TestLoadBalancer_TLSTerminatesWithSelfSignedCert` integration test: one application, one registered domain, a custom `http.Client` (`newSNIClient`) whose `DialTLSContext` always dials the load balancer's real TLS port directly while still sending the request's own hostname as SNI — exactly what a real client reaches a self-signed-fronted domain with — confirms the TLS listener reaches the right backend and that the plain-HTTP listener reaches the same backend for the same hostname. Passed on retry (one transient NATS-container startup flake on the first attempt, same known Docker Desktop flakiness precedent, then a clean 91.19s pass); Phase 4/Task 4's other three loadbalancer integration tests re-verified passing against the extended `startTestLoadBalancer` (now returns both the HTTP and TLS addresses). One unstarted `traefik/whoami` container was found left over after the full re-run (a `TestLoadBalancer_EjectsDeadBackendViaActiveHealthCheck`-scheduled replacement container that landed in Docker's `created` state right at test-end, before its DB row reached `running` and became visible to that test's own cleanup sweep — a pre-existing test-cleanup gap, not something Task 5 introduced) and removed manually; no leaks remained afterward. Full module `gofmt`/`go build`/`go vet` (default, `-tags=integration`, `-tags=e2e`) and `go test ./...` all clean. Task 6 done: `e2e/phase5_test.go` (`TestE2E_Phase5ExitCriteria`, `-tags=e2e`) automates the phase's exit criteria verbatim from `ARCHITECTURE.md` §10 — two different applications reachable via two different hostnames through the same load balancer — following the exact real-binaries-as-real-OS-processes pattern of `phase1_test.go` through `phase4_test.go`: real `apiserver`/`scheduler`/`worker`/`controller-manager`/`loadbalancer` processes against a real testcontainer Postgres+NATS+Docker daemon, driven entirely through the compiled `platform` CLI (no direct SQL, unlike the integration tests' domain-seeding shortcut) — signup → create project → `deploy app-a`/`deploy app-b` (both `traefik/whoami`) → `create domain --app app-a`/`--app app-b` → `get domains` confirms both listed → poll requests through the load balancer with each hostname's `Host` header until both resolve to two distinct backends → repeat over HTTPS via a bespoke SNI-dialing `http.Client` (`InsecureSkipVerify`, Task 5's own acceptance wording) confirming the same backend as its HTTP counterpart for each hostname → confirm an unregistered `Host` still gets 404. Needed one new helper `startLoadBalancer` (unlike every other service phase4_test.go starts via the shared `startService`, the load balancer now needs two free ports/two env vars for its plain-HTTP and TLS listeners, Task 5). Passed on first run (130.23s), no leaked containers. A later attempt to also re-run the full existing e2e suite (all 5 phase tests back-to-back in one `go test` invocation) hit severe Docker Desktop resource contention unrelated to any of this session's code — Postgres testcontainers intermittently failing to accept connections (`unexpected EOF`) even on isolated retries of the untouched, pre-existing `TestE2E_Phase1ExitCriteria` — traced to roughly a dozen unrelated long-running containers (Temporal, RabbitMQ, OpenSearch, Keycloak, Jaeger, Redis, two other Postgres instances, ages 10 hours to 2 days) already consuming the same 7.8GB Docker Desktop VM on this machine; two leaked test containers from the contended runs were found and removed. This is an environmental condition on the host, not a code regression — Phase 5's own dedicated test run (above) passed cleanly, and none of Phase 1-4's own source was touched this session. Full module `gofmt`/`go build`/`go vet` (default, `-tags=integration`, `-tags=e2e`) and `go test ./...` re-confirmed clean afterward. Phase 5's exit criteria (`ARCHITECTURE.md` §10) now pass end-to-end via this automated test — phase marked done. |
| 6 | Multi-Tenant SaaS Layer | ✅ | Started 2026-09-03, exit criteria automated and passing 2026-09-05: [`docs/phases/phase-6-multi-tenant-saas.md`](docs/phases/phase-6-multi-tenant-saas.md) task plan written and approved (dashboard UI deferral recommendation accepted — CLI parity for every feature, same as every prior phase). Task 1 done: `0010_resource_quotas.sql` (one row per org, RLS-enabled, same two-branch policy shape as `applications`) and `0011_audit_logs.sql` (append-only — `REVOKE UPDATE, DELETE` from `platform_app` at the DB layer, not just convention) migrations; `QuotaRepository` in `internal/db` (`Create`/`Get`/`CurrentUsage`, the last computing live `COUNT`/`SUM` aggregates against `projects`/`containers`-via-`deployments`/`applications` — never a cached counter) and `DefaultResourceQuota` (placeholder ceilings, same "untuned constant, revisit with real usage data" posture as every other placeholder in this codebase); `AuditLogRepository` (`Record`/`ListByOrg`, keyset-paginated on `(created_at, id)` per `api-conventions.md` §4's own cursor example, deliberately no Update/Delete method to match the DB-level guarantee). `handleSignup` extended to create each new org's quota row in the same transaction as the org/membership rows, via an explicit `SetCurrentOrg` call rather than relying on insert-ordering against the membership row's own RLS branch. Repository-level integration tests (`TestQuotaRepository`, `TestAuditLogRepository`) cover Create/Get/CurrentUsage including RLS cross-tenant isolation (an org's session asking about another org's id via an explicit function argument sees zero, not real counts — confirms the aggregate queries can't be tricked into leaking cross-org data even by a caller bug), pagination across pages, and the append-only guarantee via a direct `UPDATE`/`DELETE` attempt asserting Postgres's own `insufficient_privilege` (42501) error. Hit a genuinely persistent Docker Desktop networking issue mid-task (6 consecutive testcontainer Postgres connection failures — `unexpected EOF` on the wire-protocol handshake right as the container reported ready — reproducing even against an unmodified pre-existing test and after stopping every other container on the host to rule out resource contention; a manual diagnostic confirmed raw TCP connectivity worked fine, isolating it to Postgres's own handshake); resolved only after a full Docker Desktop restart, after which every test passed cleanly and quickly on the first attempt. Full module `gofmt`/`go build`/`go vet` (default, `-tags=integration`, `-tags=e2e`) and `go test ./...` all clean; the full `internal/db` and `services/apiserver` integration suites re-run afterward to confirm nothing else regressed (all passing; one unrelated `TestAPIServer_CoreCRUDFlow` NATS-container startup flake on a combined run, passed cleanly standalone and on a full re-run). Task 2 done: `internal/auth/rbac.go` now carries every `Permission` constant `rbac-multitenancy.md` §2 names — `PermOrganizationManage/Delete`, `PermMembersInvite/Remove/ChangeRole`, `PermApplicationScale`, `PermDeploymentRollback`, `PermEnvVarsWrite`, `PermMetricsView`, `PermAuditLogsView`, `PermAPIKeysCreate/Revoke`, `PermBillingView` — the last several constants-only, no gating route yet, matching the matrix-completeness-not-new-surface scope of this task. Resolved open decision 1: `PermApplicationScale` is now its own constant (same allow-set as `PermApplicationDeploy` today, decoupled for a future role split) and `handleScaleApplication` switched to it. Audited every existing Phase 1-5 mutating route (`projects`, `applications`, `deployments`, `domains`) against the now-complete matrix — the scale/deploy conflation just fixed was the only retrofit needed; everything else already called `requirePermission`/`requireMembership` with the matrix-correct constant. `rbac_test.go`'s `TestHasPermission_Matrix` extended to the full 20-permission × 4-role table (80 subtests) — the literal transcription of `rbac-multitenancy.md` §2 the doc calls for. Full module `gofmt`/`go build`/`go vet` (default, `-tags=integration`, `-tags=e2e`) clean; full `internal/auth` suite passing; full `services/apiserver` integration suite (`TestAPIServer_CoreCRUDFlow`/`ScaleApplication`/`Domains`) re-run clean end-to-end, confirming the scale-permission switch didn't break the live route. Task 3 done: `GET`/`PATCH`/`DELETE /v1/orgs/:orgId` — `OrganizationRepository` gains `Get`/`Update` (name only; slug stays immutable, a bigger feature than Task 3 scopes)/`Delete` (cascades through every FK chain rooted at `organizations`, all `ON DELETE CASCADE` — a known, explicitly flagged gap: nothing stops the org's actually-running containers on their nodes first, unlike `handleDeleteApplication`'s per-container unassign fan-out, out of this task's scope). `GET` is membership-only (no separate `organization.view` matrix row); `PATCH` gated by `organization.manage` (owner/admin); `DELETE` gated by `organization.delete` (owner only). Along the way, found and fixed a latent bug in `handleCreateProject` (the only other org-in-URL, no-resolution-step route): `requirePermission`'s raw `db.ErrNotFound` for a non-member was never routed through `mapDBError`, so a cross-tenant caller got an unwrapped-sentinel 500 instead of the required 404 — fixed there and built correctly into the new org routes from the start. CLI: `platform get org` and the new `platform update` command tree's `platform update org --name <name>` (no `platform delete org` yet — open decision 2, the route is tested but a CLI surface for deleting the only org a session is authenticated against needs its own story). New `TestAPIServer_Organizations` integration test: full permission-matrix check across all four roles on both `organization.manage` and `organization.delete` (including confirming admin passes the former but is denied the latter), cross-tenant denial (404, not 403) on all three routes, and confirming the cascade actually ran (a post-delete `GET` from the former owner's own still-valid token gets 404 too, since their membership row is gone). Passed clean (11.89s standalone, 19.07s/11.89s across two runs); full apiserver integration suite (`CoreCRUDFlow`/`ScaleApplication`/`Domains`/`Organizations`) re-run together, all passing (195.942s total), no leaked containers; CLI (`apps/cli/...`) build and full test suite clean; full module `gofmt`/`go build`/`go vet` (default, `-tags=integration`, `-tags=e2e`) clean. Task 4 done: `GET`/`POST`/`PATCH`/`DELETE /v1/orgs/:orgId/members[/:userId]` — `MembershipRepository` gains `ListByOrg` (joined with `users.email` for a human-identifiable roster), `UpdateRole`, `Delete`, `CountOwners`. `GET` is membership-only (no dedicated `members.view` matrix row); notably, what a caller sees back is decided by the already-shipped `memberships_isolation` RLS policy itself, not a new permission gate — an owner/admin session sees the full roster, a developer/viewer session sees only its own row (the policy's own doc comment, written back in migration `0001`, calls this out as "needed for member-management UI, Phase 6"). `POST` (invite) is owner/admin-only, existing-registered-users-only per open decision 3 (looked up by email via `UserRepository`; an unregistered email gets a clear 404 telling the caller that person needs to sign up first, not a silent no-op). `PATCH`/`DELETE` are owner/admin-only (`members.change_role`/`members.remove`) plus an application-layer last-owner guard (`CountOwners`) rejecting any remove/demote that would leave the org with zero owners — the matrix says an owner *can* do this to another owner, not that the org can end up ownerless. New `TestAPIServer_Members` integration test: full invite → list → change-role → remove flow, the permission-matrix check per route, cross-tenant denial (404), the unregistered-email 404 path, and the last-owner guard exercised on both routes plus its release once a second owner exists (a promote → demote-the-original → guard-re-engages-on-the-new-sole-owner sequence). CLI: `platform get members`, and three new top-level commands — `platform invite <email> --role <role>`, `platform remove-member <email>`, `platform set-role <email> <role>` — the latter two resolving email → user_id via a members-list lookup client-side (no local ID cache, same precedent as `delete_domain.go`'s hostname matching). Passed clean (12.66s standalone); full apiserver integration suite (`CoreCRUDFlow`/`ScaleApplication`/`Domains`/`Organizations`/`Members`) re-run together, all passing (120.210s total, faster than the prior run — Docker Desktop cooperating better this session), no leaked containers; CLI build and full test suite clean; full module `gofmt`/`go build`/`go vet` (default, `-tags=integration`, `-tags=e2e`) clean. Task 5 done: API keys. `APIKeyRepository` (`internal/db/api_key.go`) gains `Create`/`ListByOrg`/`OrgID`/`Revoke` over the ordinary RLS-scoped connection, plus `LookupActiveByHash` — the one exception, going through a new `lookup_active_api_key` SECURITY DEFINER function (`0012_api_key_lookup_function.sql`, additive rather than editing the already-shipped `0002_api_keys.sql`) since the auth path itself has no identity yet to satisfy `api_keys_isolation`'s ordinary RLS policy with, the same problem/fix migration `0001`'s `is_org_admin` already established. `auth.GenerateAPIKey`/`HashAPIKey` mint a `pk_live_`-prefixed key (rejection-sampled base62 suffix, avoiding the bias a plain modulo would introduce) and its SHA-256 hash, same discipline as `GenerateRefreshToken`. `auth.Authenticate` now takes an `APIKeyLookup` port (ADR-0011: keeps `internal/auth` independent of `internal/db`, exactly like `TokenIssuer`) and branches on the `pk_live_` prefix; a matching request's context now carries the key's own org id (`WithAPIKeyOrgID`/`APIKeyOrgIDFromContext`) alongside the creator's user id. Resolved the phase doc's "routes need a small adjustment to accept an already-known org from the API-key path" note with one centralized change rather than touching every handler: `requirePermission`/`requireMembership` (`permission.go`) now both call a new `requireAPIKeyOrgMatch` first, rejecting (404, an already-formed `*apiError` so it's correct regardless of a given call site's `mapDBError` wrapping) any API-key-authenticated request whose target org isn't the key's own — closing the gap where a key would otherwise be as powerful as its creator's full JWT across every org they belong to, not just the one org it names. Routes: `POST`/`GET /v1/orgs/:orgId/api-keys` (both gated by `api_keys.create` — no separate list permission, and only owner/admin can create anyway); `DELETE /v1/api-keys/:keyId` (deep-by-ID, gated by `api_keys.revoke`, same org-resolve-then-`SetCurrentOrg` shape as `handleDeleteDomain`). CLI: `platform create api-key <name>` (prints the plaintext key exactly once with an explicit "save this now" warning), `platform get api-keys`, `platform delete api-key <name>` (resolved by name via list, same precedent as `delete domain`). New `TestAPIServer_APIKeys` integration test: full create/list/revoke permission-matrix check, cross-tenant denial (404), a revoked key's own requests rejected outright (401) rather than merely denied downstream, the plaintext key confirmed absent from every list response, and — the auth-path wiring itself — an existing, unrelated route (`GET .../deployments`) successfully authenticated end to end via a plaintext API key instead of a JWT, plus a dedicated org-scope-narrowing check (the key's creator is made a real member of a second org via JWT, and the key still can't reach that org's resources, contrasted against the same creator's JWT succeeding on the identical request). Found and fixed one bug along the way: `APIKeyRepository.Create` passed a `nil` scopes slice straight through as a query parameter, binding as SQL `NULL` instead of falling through to the column's `NOT NULL DEFAULT '{}'` — fixed by coercing `nil` to `[]string{}` before the insert. Passed clean (47.89s standalone); full apiserver integration suite (`CoreCRUDFlow`/`ScaleApplication`/`Domains`/`Organizations`/`Members`/`APIKeys`) re-run together, all 6 passing (139.570s total), no leaked containers; CLI build and full test suite clean; full module `gofmt`/`go build`/`go vet` (default, `-tags=integration`, `-tags=e2e`) clean. Task 6 done: quotas, three-layer enforcement (`ARCHITECTURE.md` §2.9). **Layer 1** (`services/apiserver/internal/server/quota.go`) — `checkProjectQuota`/`checkDeploymentQuota`/`checkResourceQuota`, wired into `handleCreateProject`, `handleCreateDeployment`, and `handleScaleApplication`; a violation returns the new `errQuotaExceeded` (`422`, `code: "quota_exceeded"`, same shape as `errLastOwner`). `checkResourceQuota`'s container/CPU/memory check subtracts the target application's own *current actual* container contribution (via `DeploymentRepository.Latest` + `ContainerRepository.CountActiveByDeployment`) before adding back its post-action contribution, so a steady-state redeploy at unchanged replica count doesn't double-count against the org total while a first-ever deploy (zero actual containers regardless of `replicas_desired`) is still caught correctly. New `GET /v1/orgs/:orgId/quota` (membership-only, no dedicated `quota.view` matrix row, same precedent as `GET .../members`) backing `platform get quota` (CLI-only, no `platform set quota` — limits are platform-operator-set per the phase doc's own open decision). **Layer 2** (`services/scheduler/internal/scheduler`) — `handlePlacement` re-checks quota (`Scheduler.checkQuota`) immediately after `selectNode` succeeds and before the `containers` row is written, closing the TOCTOU gap between the API's check and a container actually landing; rejects on `usage.Containers+1 > quota.MaxContainers` or on CPU/memory already over budget (relevant if an operator lowers a quota below already-committed resources). Needed a second, superuser (`platform`) admin connection for the scheduler (`SCHEDULER_ADMIN_DATABASE_URL`), since `resource_quotas` and `deployments` carry RLS and the scheduler has no per-request tenant session — the same dual-connection precedent `controller-manager`'s `ReconcileRepository` and the load balancer's `DomainRoutingRepository` already established; `nodes`/`containers` stay on the scheduler's existing ordinary connection since neither carries RLS. `DeploymentRepository` gains `OrgID` (same convention as `ApplicationRepository`/`ProjectRepository`/`DomainRepository`/`APIKeyRepository`) to resolve org from `deployment_id` for this check. **Layer 3** (`0013_quota_enforcement_trigger.sql`, additive per the established practice of never editing an already-shipped migration) — `SECURITY DEFINER` trigger functions `enforce_container_quota()`/`enforce_project_quota()` on insert into `containers`/`projects`, rejecting via `RAISE EXCEPTION ... USING ERRCODE = 'check_violation'` (SQLSTATE `23514`) if the owning org's post-insert count would exceed its `resource_quotas` row; `SECURITY DEFINER` is essential here (not just the usual self-referential-RLS-escape reason) because this backstop specifically has to defend paths — the scheduler's admin connection, a raw bypass insert — that never set the ordinary `app.current_org_id`/`current_user_id` session state a session-dependent check would need. New integration tests: `TestAPIServer_Quotas` (Layer 1, Postgres+NATS only) covers the boundary for project/deploy/scale plus the `GET .../quota` read and cross-tenant 404; `TestScheduler_RejectsPlacementOverQuota` (Layer 2, Postgres+NATS only, no worker needed) confirms a second placement for an already-at-ceiling org never produces a second `containers` row; `TestQuotaEnforcementTriggers` (`internal/db`, raw admin `*sql.DB`, no app code at all) confirms both triggers reject a direct SQL insert past the boundary with SQLSTATE `23514`; `TestAPIServer_QuotaThreeLayers` (full stack — Postgres+NATS+real scheduler+real worker) drives one real deploy to its container-quota boundary via actual placement, then confirms all three layers independently reject the same next unit of work at that exact boundary (Layer 1 via a scale `PATCH`, Layer 2 via a raw second `placement.requested` publish, Layer 3 via a raw SQL insert). Two real bugs found and fixed along the way (not flakes): (1) `TestAPIServer_Quotas`'s quota-lowering `UPDATE resource_quotas` was originally issued over a bare, session-less connection against a `FORCE ROW LEVEL SECURITY` table, silently matching zero rows and never actually lowering the quota — fixed by routing it through `pool.WithTx` with the org owner's session, same as every other RLS-bound write in this codebase; (2) adding Layer 2's `checkQuota` exposed a real, pre-existing gap in `services/scheduler/main_integration_test.go`'s `seedDeployment` helper — it creates an org via raw SQL but (unlike real `handleSignup`, which always creates one atomically) never gave it a `resource_quotas` row, so `TestScheduler_PlacesDeploymentOnHealthyWorker` (unrelated to quotas, pre-dating Task 6) started failing once Layer 2 unconditionally rejected placements for orgs with no quota row — fixed by having `seedDeployment` itself seed a generous quota row, restoring the same invariant every real org has. Passed clean after both fixes (`TestScheduler_PlacesDeploymentOnHealthyWorker` 56.29s, `TestScheduler_RejectsPlacementOverQuota` 27.70s, full scheduler suite 89.208s); full `services/apiserver` integration suite, full `internal/db` integration suite, CLI build/tests, and full module `gofmt`/`go build`/`go vet` (default, `-tags=integration`, `-tags=e2e`) all clean. Several transient Docker Desktop connection flakes hit both new and unrelated pre-existing tests during this task (same documented environmental precedent as earlier phases); all passed cleanly on retry with no code changes, and the environment self-recovered without needing a Docker Desktop restart. Task 7 done: audit log write path + read route. A new `recordAudit` helper (`services/apiserver/internal/server/audit.go`) writes one `audit_logs` row over the same `conn` — and therefore inside the same transaction — as the mutation it records, per the phase doc's open decision 4 (synchronous/in-transaction, not NATS-subscriber-driven, so there's no risk of a lost event silently producing an incomplete trail). Wired into every mutating route across the whole API surface: signup (`organization.create`), project create, application create/scale/delete, deploy, domain create/delete, organization update, member invite/change-role/remove, and API key create/revoke — each with a small `metadata` payload (e.g. `application.scale`'s before/after replica counts), never including secrets (an API key's plaintext/hash never appears in `api_key.create`'s metadata). One deliberate, documented exception: organization *delete* does not record an entry — `audit_logs.org_id` is `ON DELETE CASCADE` from `organizations`, so a row written in the same transaction that deletes its own org would be erased by that very statement, making it worse than not writing one (a real "org was deleted" trail needs `audit_logs` decoupled from that FK, out of this task's scope, same category as the container-cleanup gap Task 3 already flagged for this route). New `GET /v1/orgs/:orgId/audit-logs?limit=&cursor=`, gated by the already-existing `audit_logs.view` permission (owner/admin only), cursor-paginated identically to `handleGetDeployments`/`AuditLogRepository.ListByOrg`'s own shape — the literal route `api-conventions.md` §4 uses as its cursor-pagination worked example. CLI: `platform get audit-log` (`--cursor` to page further back). New `TestAPIServer_AuditLogs` integration test (Postgres only, no NATS/scheduler needed — every audited action here commits before any cross-service call): one representative action each from projects/applications/domains/orgs/members/api-keys produces a matching row with the right `target_id`; the read route's permission matrix (owner/admin allowed, developer/viewer denied) and cross-tenant denial (404); and a direct `UPDATE`/`DELETE` against `audit_logs` through the API server's own (non-superuser `platform_app`) connection still rejected with `insufficient_privilege` — re-confirming Task 1's DB-level guarantee end-to-end through the API's own connection role, not just `internal/db`'s own test. Passed clean (8.69s standalone); full apiserver integration suite (all 9 tests, including the new one) re-run together — 8 passed on the first pass, `TestAPIServer_Quotas` (pre-existing, untouched by this task) hit the same documented Docker Desktop connection-flake pattern (`unexpected EOF`/`nats ... EOF`) twice in a row before passing cleanly on a third standalone retry (17.78s), confirming environmental, not a regression; no leaked containers. CLI build and full test suite clean; full module `gofmt`/`go build`/`go vet` (default, `-tags=integration`, `-tags=e2e`) and `go test ./...` all clean. Task 8 done: the required RBAC/tenant-isolation test sweep (`rbac-multitenancy.md` §5), applied as one consolidated audit across every resource type built by now — a coverage audit/consolidation task per the phase doc's own framing, not new product surface. Audited every existing test against §2's full permission matrix and found real gaps: `TestAPIServer_CoreCRUDFlow`/`ScaleApplication` only ever exercised the owner role plus a handful of cross-tenant cases, never running the other three roles against `project.create`/`application.create`/`application.delete`/`application.deploy`/`application.scale` at all — `TestAPIServer_ScaleApplication` in particular had zero permission or cross-tenant coverage whatsoever, purely a functional happy-path test. New `services/apiserver/internal/server/rbac_sweep_test.go` closes every gap the audit found in one place: `TestAPIServer_RBACPermissionMatrixSweep` (Postgres+NATS, no scheduler/worker needed — only the permission check and the deploy route's publish-success path are under test, not real placement, same reasoning `TestAPIServer_Quotas` already established) runs project.create/application.create/delete/deploy/scale through all four roles plus a cross-tenant stranger as one parent test with a `t.Run` per row, and also fills two smaller pre-existing gaps: `domains.manage`'s untested admin case (`TestAPIServer_Domains` only ever checked developer/viewer) and `logs.view`'s viewer-is-not-forbidden case (`handleGetLogs` gates on `requireMembership`, not `requirePermission`, since every role is allowed — confirmed by observing a viewer's request reach the "never been deployed" 404 rather than being stopped at 403). The file's own doc comment is the "single place" the phase doc's acceptance criteria asks for: a literal row-by-row account of every §2 permission's coverage, cross-referencing the existing tests that already cover the rest (`organization.*`/`members.*`/`domains.manage`'s core cases: Tasks 3-4-Domains; `audit_logs.view`/`api_keys.*`: Tasks 5/7) and naming the permissions with no live route yet (`deployment.rollback`, `env_vars.write`, `metrics.view`, `billing.view`, `project.delete`) as covered only at the abstract level by `internal/auth/rbac_test.go`'s existing `TestHasPermission_Matrix` (all 20 permissions × 4 roles) — the only test that can exist for a permission nothing gates yet. New `TestAPIServer_RoleChangeTakesEffectImmediately` closes §5's third required item, previously untested anywhere: one user's membership is demoted mid-session using their own real, never-reissued/never-refreshed JWT from signup — the very next request is denied immediately, then promoted back mid-session with the same token — the very next request succeeds again immediately, both proving `requirePermission`'s already-existing "always re-check `RoleForUser` from Postgres, never trust the token" design (ADR-0008) holds end to end, not just by inspection. Both new tests passed clean on retry (one transient NATS-connection flake on the sweep test's first attempt, same documented Docker Desktop precedent, clean on retry: 10.17s and 5.45s respectively); the full apiserver integration suite re-run together afterward — all 12 tests passing in one run (243.836s total), no flakes, no leaked containers. CLI build/vet/test and full module `gofmt`/`go build`/`go vet` (default, `-tags=integration`, `-tags=e2e`) and `go test ./...` all clean. Task 9 done: `e2e/phase6_test.go` (`TestE2E_Phase6ExitCriteria`), automating the phase's own exit criteria exactly as `phase1_test.go` through `phase5_test.go` did for theirs — the last task in Phase 6. Script: owner A signs up and creates a project via the real CLI; owner B signs up separately (the "two orgs" half of the script); a third, already-registered user D is invited into org A as `developer` (open decision 3's existing-registered-users-only invite, exercised via `platform invite`). Every request D makes from there goes over raw `net/http` with a bare bearer token, never through the CLI — the CLI's session model tracks exactly one current org per signup/login (`cfg.OrgID`), so a user who's a member of two orgs (their own from signup, plus the one they were invited into) has no CLI verb to act as the invited role in the second org anyway; raw HTTP is therefore not just the exit criteria's own "even if the frontend check is bypassed" wording, it's the only way to drive this scenario at all. Asserts, in order: a sanity read of D's own org A succeeds (200, proving the token/membership actually work before trusting any denial below); the cross-tenant scenario — D reading org B (404) and writing a project into org B (404), never a member, don't-leak-existence per `rbac-multitenancy.md` §5; and the literal exit-criteria assertion — D's `PATCH /v1/orgs/:orgId` against org A itself (`organization.manage`, same org, wrong role) rejected 403, contrasted against the identical PATCH succeeding when owner A performs it through the CLI. Passed clean on its first standalone run (39.71s) and twice more on later reruns (53-73s each) with zero code changes between them. While verifying no regression across the full e2e suite, found and fixed a real, pre-existing bug from Task 6: the scheduler has required a second, superuser `SCHEDULER_ADMIN_DATABASE_URL` connection since Task 6's Layer 2 quota re-check, but the shared e2e `startScheduler` helper (`phase1_test.go`, reused by every phase's e2e test) was never updated to provide one — every phase1-5 e2e test's scheduler process was silently falling back to a hardcoded `localhost:5432` default and refusing to start against the ephemeral testcontainer port, a 100%-reproducible failure that had gone unnoticed because the full e2e suite hadn't been re-run since Task 6 shipped. Fixed by threading `adminDBURL` (already returned by `startPostgres`, previously discarded with `_` in phase1_test.go/phase2_test.go) through `startScheduler` and all five call sites (phase1-5); validated by re-running the fixed suite — `TestE2E_Phase3ExitCriteria` passed cleanly end to end (162.21s), and every other phase's scheduler log confirmed successful admin-connection + placement on every subsequent run (previously 100% failing, now 100% succeeding on that specific failure mode). The remaining phase1/phase2 timing-flakiness seen during this verification (container placement occasionally exceeding their tests' fixed 30s CLI-poll windows, plus a recurring Postgres testcontainer `unexpected EOF` handshake flake matching the documented precedent from earlier phases) was traced to a concrete, unrelated cause: `docker stats` showed a different project's `crm-opensearch-1` container consuming ~1691% CPU (~17 cores) on the host throughout this session, starving Docker Desktop's own VM/networking — not a regression in any Phase 6 code, and out of this task's scope to paper over by loosening unrelated phases' test timeouts. Full module `gofmt`/`go build`/`go vet` (default, `-tags=integration`, `-tags=e2e`) and `go test ./...` all clean. **Phase 6 complete** — all 9 tasks done. |
| 7 | Deployment Platform (Git → build → deploy) | 🟨 | Started 2026-09-11: [`docs/phases/phase-7-deployment-platform.md`](docs/phases/phase-7-deployment-platform.md) task plan written and approved. Task 1 done: added `ARCHITECTURE.md` §2.10 (Image Builder) closing the gap where every other component had a `§2.x` home but this one only had a repo-layout stub and a modularity-registry row; `0014_builds.sql` migration (`builds`, RLS-enabled, same two-branch policy shape as `domains` — tenant-authored config, not cluster-observed state) and `BuildRepository` in `internal/db` (Create/ListByApplication/OrgID/UpdateResult, cursor-paginated identically to `AuditLogRepository.ListByOrg`). New `TestBuildRepository` integration test covers Create (including the empty-`git_ref`-defaults-to-`main` case), cursor pagination across pages, RLS cross-tenant isolation on both `ListByApplication` and `OrgID`, and `UpdateResult`'s succeeded/failed shapes plus its nonexistent-id-fails-closed case — passed clean (8.98s), migration applies cleanly on top of 0001-0013. Task 2 done: added `registry:2` (unauthenticated, local) as a new `registry` service in `infrastructure/docker-compose.yml` (`REGISTRY_PORT`, default 5000), same local-dev-is-a-logical-simulation posture ADR-0009 already established; `docs/local-development.md` §2's aspirational service list extended to match. Verified live: `docker compose up -d registry`, then a real `docker push`/`rmi`/`pull` round-trip against `localhost:5000` confirmed it's reachable and unauthenticated; container stopped afterward, no leftover state. Full module `gofmt`/`go build`/`go vet` (default, `-tags=integration`, `-tags=e2e`) and `go test ./...` all clean. Task 3 done: `services/image-builder/internal/imagebuilder` — the `ImageBuilder` port (adapted to this package path rather than the modularity registry row's literal bare `services/image-builder/internal`, to match every sibling service's own `internal/<name>` subpackage convention) plus its one adapter, `DockerBuilder`: clones a repo (shells out to the system `git` binary — no named port, per `modularity-and-extensibility.md` §6's "no interface without a concrete alternative" rule — resolving the checked-out commit's SHA), tars the working tree (`.git` excluded) as a Docker build context, builds via the Docker Engine SDK's `ImageBuild` tagged `<repository>:<commit_sha>`, and pushes anonymously (`base64("{}")` `X-Registry-Auth`, matching the unauthenticated local registry from Task 2); both `ImageBuild`/`ImagePush`'s JSON-stream responses are drained for an inline `{"error":...}` object, since the HTTP status alone stays 200 even when a build/push fails partway through. New `build.requested`/`build.completed` NATS subjects (`internal/eventbus/subjects.go`, new `BUILDS` JetStream stream covering both — one subject for both build outcomes, mirroring `node.<id>.status`'s existing "status field carries the branch" precedent) and a `Consumer` tying them to the `ImageBuilder` port; `services/image-builder/main.go` wires `DockerBuilder`+`Consumer` together, NATS treated as a hard dependency (same posture as scheduler's `main.go`, unlike the worker's degrade-to-HTTP-only). New `TestDockerBuilder_ClonesBuildsAndPushes` integration test: a real, hermetically-fixtured one-commit Git repo (no live network dependency) built and pushed against a real `registry:2` container, confirming the returned commit SHA/image tag are correct and the exact pushed tag is actually pullable back down; a nonexistent ref and a Dockerfile with a nonexistent base image each confirmed to produce a real error, not a hang or silent success. Found and fixed a real environment-specific flake during development, not a code bug: pushing to the test registry via an *ephemeral* host port reliably failed with `connection refused` from the *daemon's own* push request (`ImagePush` executes inside `dockerd`, not the test process) even though this test's own HTTP client could reach the identical address a moment earlier — Docker Desktop's VM-based daemon not yet covering a freshly-assigned high port via its loopback DNAT rules; fixed by using a fixed host port for the test registry, matching the exact configuration Task 2 already verified working end-to-end. Full module `gofmt`/`go build`/`go vet` (default, `-tags=integration`, `-tags=e2e`) and `go test ./...` all clean; no leaked containers. Task 4 done: API server wiring for the git path. `handleDeploy`'s core split into two shared helpers — `createDeploymentTx` (bump the application's image if this deploy changes it, create the `deployments` row, audit it, all inside the caller's own transaction so the Layer 1 quota check and the write stay atomic) and `publishPlacement` (marshal + publish `placement.requested` outside any transaction per ADR-0012, marking the deployment `failed` on a publish error) — so the HTTP `--image` path and the new `build.completed` consumer run one sequence rather than two hand-copied ones. `POST /v1/applications/:appId/deployments` now accepts `git_url`/`git_ref` as an alternative to `image`: same `application.deploy` permission (`rbac-multitenancy.md` §2 doesn't distinguish how a deploy was triggered) and the same `checkDeploymentQuota`/`checkResourceQuota` pre-check at build-*request* time, then creates a `builds` row, audits `build.create`, publishes `build.requested`, and answers `202 Accepted` with the build to poll — no deployment exists yet. New durable consumer `apiserver-build-completed` on `build.completed` (apiserver's first-ever NATS *subscription*; every prior interaction was publish-only, so `main.go` degrades a subscribe failure exactly as it already degrades an unreachable bus — the git path stops working, every other route keeps serving): on `succeeded` it records the outcome and runs the two shared helpers to produce an ordinary deployment, using the build row's own `created_by` as the audit actor (no request-scoped session exists, same posture `ReconcileRepository` already establishes); on `failed` it only updates `builds`, with no deployment and no audit entry (Phase 6 Task 7's "system state transitions aren't audited, only the initiating user action is" line drawn between "a user requested a build" and "the build finished"). New `GET /v1/applications/:appId/builds`, `requireMembership`-only and cursor-paginated identically to `handleGetDeployments`. Three deviations from the task plan as written, each deliberate: **(1)** the plan's "`errBadRequest` if both **or neither**" would have been a real regression — `handleDeploy` has always treated an absent `image` as "redeploy the application's current image" and the CLI documents `--image` as required only on first creation, so only the *both-set* case is rejected; **(2)** the `build.completed` consumer can't read the RLS-protected `builds` row it's told about, since a NATS message arrives with no session and the consumer knows only a build id — rejected giving apiserver a superuser pool (an RLS-bypassing connection inside the internet-facing service is the one place ADR-0010's posture least tolerates it) and a `SECURITY DEFINER` lookup (a permanent RLS-bypass surface for something a wire field solves for free) in favour of echoing `org_id` through `build.requested` → `build.completed` as an opaque correlation field, used *only* to scope the transaction while every value acted on (`application_id`, `created_by`, `org_id`) is read off the verified row; this fails closed — a forged `org_id` leaves RLS with no row to return for that build id — and is covered by a dedicated `BuildRepository.Get` test asserting exactly that, including the org-hint-only/no-`current_user_id` session shape the consumer really uses; **(3)** `docs/nats-contract.md` had never received the `build.requested`/`build.completed` rows Task 3's own `subjects.go` cites it for, so this task closed that gap (transport table, both payload schemas, the `BUILDS` stream row, and the "hint, not authority" reasoning above). Also added `BuildRepository.Get` and `Server.WithImageRegistry` (an option rather than a sixth positional parameter on `New`, keeping `IMAGE_REGISTRY_ADDR` config ownership in `main.go` while leaving ten-plus existing `New` call sites untouched). New coverage: `TestAPIServer_GitDeploy` (mutual exclusion → 400; a git request creates a pending build, publishes a `build.requested` carrying the right ids and Open Decision 3's `<registry>/<org>/<app>` repository, and creates no deployment; a synthetic `succeeded` completion records commit SHA/image and produces a deployment with that image plus a matching `placement.requested`; a `failed` one records the reason and never deploys — no image-builder process involved, the same "don't stand up every downstream service" posture `TestAPIServer_Quotas` established), `TestConsumer_PublishesOutcomes` (image-builder's consumer had *no* test at all — Task 3 covered only `DockerBuilder` — so the `org_id` echo the whole Task 4 design rests on would have gone unverified until Task 6's e2e run), and RBAC sweep rows for `application.deploy`'s git mode (matrix + cross-tenant 404) and `GET .../builds`. Found and fixed one bug of my own along the way: each subtest in the new image-builder consumer test attaches a fresh durable consumer, which replays the stream from the start, so the second subtest was asserting against the first's message until the observer was filtered by build id. Full apiserver integration suite passes (124.28s, including the real-scheduler/real-worker `CoreCRUDFlow`), `TestBuildRepository` 7.25s, `TestConsumer_PublishesOutcomes` 4.50s; full module `gofmt`/`go build`/`go vet` (default, `-tags=integration`, `-tags=e2e`) and `go test ./...` all clean, no leaked containers. Next: Task 5 (CLI — `deploy --git`/`--ref`/`--dockerfile` + `get builds`). |
| 8 | Advanced Deployment (rolling, rollback) | ⬜ | |
| 9 | Observability | ⬜ | |
| 10 | Reliability (failure injection) | ⬜ | |
| 11 | Production Hardening | ⬜ | |

## Next action

Phase 7 (Deployment Platform) is 🟨 in progress — task plan approved, Tasks 1-4 done (see the Phase 7 row above). Next: Task 5 — CLI (`platform deploy <app> --git <url> [--ref] [--dockerfile]` as an alternative to `--image`, plus `platform get builds <app>`) per [`docs/phases/phase-7-deployment-platform.md`](docs/phases/phase-7-deployment-platform.md).

## How to update this file

- Flip a phase to 🟨 when its first task starts, ✅ only when its exit criteria (`ARCHITECTURE.md` §10) actually pass.
- Add a one-line note on anything that deviated from the written plan — if a phase's actual implementation diverges from `ARCHITECTURE.md` or its ADRs, update those documents in the same change, don't just note the drift here and move on.
