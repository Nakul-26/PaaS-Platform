// Command scheduler places workloads onto worker nodes (ARCHITECTURE.md
// §2.2). Not built until Phase 2 — Phase 1 has exactly one worker, called
// directly by the API server.
package main

import "log/slog"

func main() {
	slog.Info("scheduler: not yet implemented")
}
