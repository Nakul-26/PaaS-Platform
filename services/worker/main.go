// Command worker is the per-node agent that drives containers via the
// ContainerRuntime interface (ARCHITECTURE.md §2.4). Implementation lands in
// Phase 1 Task 4.
package main

import "log/slog"

func main() {
	slog.Info("worker: not yet implemented")
}
