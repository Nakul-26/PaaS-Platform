// Command image-builder builds and pushes container images from source
// (ARCHITECTURE.md §2 repo structure). Not built until Phase 7.
package main

import "log/slog"

func main() {
	slog.Info("image-builder: not yet implemented")
}
