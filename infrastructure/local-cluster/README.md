# infrastructure/local-cluster

Scripts to spin up N simulated worker-agent processes against one Docker daemon, per
`ARCHITECTURE.md` §6 (logical nodes, shared daemon) and `ADR-0009`.

Empty until Phase 2 (multi-node scheduling) — Phase 1 runs a single worker.
