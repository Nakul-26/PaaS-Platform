# proto (deferred)

Protobuf contracts for internal gRPC, once gRPC is introduced (`ADR-0007`, Phase 2+).

Empty until then — Phase 1 uses plain HTTP/JSON for the API server ↔ worker agent contract
(`docs/phases/phase-1-mvp.md` Task 4).
